<center>
    <% if @merchant %>
    <h1>All Items From <%= link_to @merchant.name, "/merchant/#{@merchant.id}" %></h1>
    <hr>
  <% else %>
    <h1>All Items</h1>
    <hr>
  <% end %>

  <%= render 'index' %>
</center>
